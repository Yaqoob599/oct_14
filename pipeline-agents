pipeline{
       agent none
       stages {
           stage('stage-1') {
                 agent { label 'master' }
                 steps {
                       sh 'mkdir java.txt'
                 }
          }

          stage('stage-2') {
                agent { label 'java'}
                steps {
                      sh 'touch sample.txt'
                }
          }
       }
}
